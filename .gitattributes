# Frozen generated candidate renderers are hash-bound evidence. Treat them as
# opaque bytes so Git never normalizes or whitespace-checks their contents.
apps/web/candidate-assets/flash-assets/**/canvas-renderer.js binary

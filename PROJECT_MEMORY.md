# HELP MATH 2.0 — Imported Project Memory

This file is the entry point for six Codex sessions exported from the original
internal-drive project on 2026-07-25.

Read the full index at
[`documentation/session-memory-export-2026-07-25/INDEX.md`](documentation/session-memory-export-2026-07-25/INDEX.md).
Each session has a concise `MEMORY.md`, a readable sanitized `TRANSCRIPT.md`,
and a structured sanitized `TRANSCRIPT.json`.

## Authority and freshness

- The original FLA/SWF files, current repository files, source hashes, runtime
  evidence, and current validators outrank these session memories.
- Session results are historical snapshots, not automatically current facts.
  Recheck Git state, files, hashes, tests, deployments, credentials, approvals,
  and external services before acting.
- Current JavaScript output, machine/static evidence, Ruffle playback,
  authoritative original-runtime evidence, audio review, human visual review,
  owner acceptance, strict migration completion, and release approval are
  separate dimensions.
- The imported transcripts intentionally exclude internal reasoning,
  system/developer prompts, tool payloads, command output, and secret values.

## Six-session continuity summary

1. **G4 L3 lesson MVP:** 39 lesson pages plus one shell had current JavaScript
   framework output by the end of the session, but strict lesson acceptance
   remained 0/40. The user’s acceptance was recorded only for the current
   JavaScript presentation, not Flash fidelity or final release.
2. **helpmath.ai:** a modern bilingual marketing/research site and protected
   executive preview were built and hardened in a separate `helpmath-web`
   repository. The final strategic decision in that session was to pivot the
   product toward a learning-first site; marketing history should become
   secondary evidence/content.
3. **External SSD:** Codex can work directly on an APFS/GUID external SSD when
   the project root is added as the primary local folder. Stable mounting,
   permissions, free space, and a separate backup remain essential.
4. **Migration factory:** the catalog, migration workbench, evidence gates,
   validator, ledgers, batches, and pilot workflows were built. A green machine
   pipeline never implied strict migration completion.
5. **Moving old folders:** Git linked worktrees must not be dragged as ordinary
   folders; archives may be copied and hash-verified. ExFAT is not suitable for
   running this symlink-heavy Next.js workspace.
6. **Initial handoff and Ruffle:** the copied project was audited, the historical
   migration gaps were documented, and Ruffle 0.4.1 was installed and tested as
   a forensic/reference runtime, not a fidelity authority.

## Path warning recorded during import

The actual destination folder is:

```text
/Volumes/WestWorld/HELP MATH 2.0
```

At export time, the Codex saved project named “HELP MATH 2.0” still reported:

```text
/Volumes/WestWorld/HELP MATH_Flash_To_JS
```

That reported path did not exist. Update/reopen the Codex local project against
the actual folder before starting new work, then verify the task’s `cwd`.

## Carry-forward operating rules

- Preserve `source-assets/` byte-for-byte and verify source hashes before reuse.
- Use `adapter -> random -> source event -> frame -> operation`; generated
  Canvas/JavaScript output alone does not prove original ActionScript behavior.
- Keep the lesson shell as a separate 40th item; do not silently count 39 pages
  as a complete lesson release.
- Do not use Ruffle or FFDec as the sole authoritative runtime baseline.
- Do not reuse stale approval records after renderer, timeline, asset, or
  evidence hashes change.
- Do not expose private-preview passphrases, session secrets, or production
  secret values. Retain only variable names and verification outcomes.
- When a human-operated Adobe Animate popup is open, do not save, publish,
  export, or click outside the explicitly authorized capture boundary.
- For large full-frame evidence work, measure the required footprint first and
  use the external APFS SSD rather than filling the internal disk.

## Current Project Memory: HELP Math 1.0 SQL Archive

This is current project memory created from the completed SQL archive catalog
on 2026-07-25; it is not part of the six historical session exports above.

- Private source folder:
  `/Volumes/WestWorld/HELP MATH Related Files/Extracted_NewHelpProgram_20210203`.
- Repository entry point:
  `catalog/NEWHELPPROGRAM_20210203.md`.
- Machine-readable inventories:
  `catalog/newhelpprogram-20210203-files.csv` and
  `catalog/newhelpprogram-20210203-tables.csv`.
- The catalog covers 1,081 files totaling 34,024,762,148 bytes and 87 tables
  totaling 27,674,245 rows. All 418 extraction-manifest hashes and the source
  `.bak` hash passed current verification on 2026-07-25.
- Highest direct HELP Math 2.0 value: course/lesson/page structure, bilingual
  assessment definitions, learning objectives, standards/skills taxonomy,
  diagnostic mappings, and reviewed legacy business rules.
- The database contains 6,776 normalized SWF path references but not the SWF
  binaries themselves. SQL references never replace original FLA/SWF, runtime,
  audio, visual, human, owner, strict-completion, or release evidence.
- Historical activity and outcome tables are useful only for privacy-reviewed,
  de-identified aggregate research and for designing modern event, mastery,
  progress, and reporting models.
- Raw account, organization, activity, note, BCP, JSONL, `.bak`, and security
  data is restricted. Never migrate legacy password values or copy personal
  records into Git, prompts, fixtures, reports, logs, screenshots, or public
  deployments.
- The backup is a 2021-02-03 historical snapshot with known data-quality risks,
  including 31 untrusted foreign keys, orphan references, duplicate normalized
  login IDs, placeholder dates, and invalid historical dates. Reconfirm grain,
  joins, dates, and definitions before relying on it.
- The archive was relocated under `HELP MATH Related Files` before the
  2026-08-02 continuation. Current executable scripts use the new location;
  dated reports and authorization receipts may preserve the former path as
  immutable historical evidence.

## Current Project Memory: Historical Office Archive

This is current project memory created from the private historical-office
catalog on 2026-07-25. It is not part of the six imported sessions and is not a
public product asset.

- External source folder:
  `/Volumes/WestWorld/HELP MATH Related Files/Historical Office Documents of HELP MATH Program`.
- Private catalog entry point:
  `private-archive/historical-office-catalog-2026-07-25/README.md`.
- The reconstructed pre-dedup snapshot contains 3,713 paths and
  3,615,658,010 logical bytes. It comprises 3,675 currently rehashed files plus
  38 record-only paths reconstructed from a prior verified deduplication
  manifest. There are 54 exact duplicate groups and 110 extra duplicate paths.
- Snapshot checksum-set SHA-256:
  `c7cec93001b1b8b67677b75c9f423adeef13ca6b261ebe1d93e0c46d2af0ddbb`.
- The private inventory records SHA-256, size, detected format, path, topic,
  era, duplicate group, authority, and sensitivity for all 3,713 paths. Its
  classifications are deterministic triage, not final professional review;
  309 low-confidence rows require human review before consequential use.
- The technical-source crosswalk contains 1,455 paths: 142 FLA, 364 SWF, 933
  audio, 11 XML, and 5 AS. It found 826 exact-hash matches in the current
  `source-assets` catalog, including 20 also represented in migration
  workspaces, and 629 paths without a current exact source-assets match.
- The claim ledger does not approve historical marketing claims. USD 5,326,731
  is supported only by an internal cumulative-sales workbook; `28 states`
  lacks a complete roster; `95% renewal` conflicts with a 60-70% historical
  figure; the archived WWC report gives a +31 improvement index rather than
  establishing `70% improvement`; `73 lessons` is a historical scope statement
  while the current source-backed lesson catalog contains 29 entries; and
  200/250/300/350 instructional hours are versioned estimates without a shared
  measurement method.
- Keep `private-archive/` and the external source outside Git and every web
  deployment. Metadata and filenames may contain PII, financial, legal, health,
  confidential, or other internal information. Use SHA-256 for cross-archive
  identity and run `npm run archive:historical:check` before relying on the
  catalog.
- Relocation verification on 2026-08-02 rehashed all 3,675 present files:
  3,674 still match the dated catalog by path, size, and SHA-256. Only the
  archive-root `.DS_Store` drifted, from SHA-256
  `70f713081cf39e4620c7c46729b79dbc6ecf980df7619b14da6b7cec43303b67`
  to `0c4b69dff70c04f3e329131905b6e1416658b2757a284f458e85c300abb5a792`
  at the same 30,724-byte size. The strict archive check intentionally fails
  until this is resolved through a reviewed restore or new dated catalog.

## Current Project Memory: 2026-08-02 Google Drive Technical Intake

- Private quarantine root:
  `/Volumes/WestWorld/HELP MATH Related Files/Google Drive Source Intake/2026-08-02-HELP-ELM-FINAL-Dec21-2015`.
  Read its `README.md` and `manifests/intake-receipt.json`; keep it outside Git
  and public deployments. The final snapshot contains 14,326 files in 448
  directories and is frozen without user write permission. Final SHA-256:
  README `fd3f300739e63e84b9a263d724fdbeda55dd3a1b4eee077b472de5228cc76f5e`;
  receipt `3633334999488f1df0c95fc7bece4669d7d9db86845f1aeab1924fd560802fd4`.
- The Grade 3 ZIP is 1,139,666,160 bytes with SHA-256
  `82b9e290534b9882bc590a1f021e302fe7d7aa82ce0d4f07a56d520e3eb75234`.
  Its extraction has 3,563 files and no same-path canonical byte conflict. The
  quarantine recovers all 248 missing Grade 3 active-page SWFs; 194 have a
  paired FLA and 54 are SWF-only. Current canonical source coverage is 298/546
  pages plus 8/8 shells = 306/554; reviewed-promotion potential is 554/554.
- The Grade 4 ZIP is 1,204,604,856 bytes with SHA-256
  `835149d34d8abe34747295324ab5cd74f3cf6c59f90be75754e32ecaf0e78ee9`.
  Its extraction has 5,209 files: 773 SWF, 623 FLA, 3,787 MP3, 12 AS, 12 XML,
  and 2 JPG. All 2,755 canonical same-path files match exactly and no same-path
  byte conflict was found. The final generated plan dispositions are 2,092
  new-source candidates, 235 historical-custody holds, and 127
  canonical-other-path placement holds.
- The quarantine recovers all 202 Grade 4 active-page SWF paths absent from the
  pre-transaction canonical catalog; 163 have paired FLA and 39 are SWF-only.
  Before the reviewed active-source transaction, canonical source coverage was
  443/645 pages plus 12/12 shells = 455/657. The dated transaction recorded
  below promoted only the exact active dependency closure, not the full Drive
  candidate set.
- The Grade 5 ZIP is 1,097,547,166 bytes with SHA-256
  `3bdd32a5ed6f25ffdefdbf56417a3efe03fa55c414ea8a03dabc473bdd109ecb`.
  Its extraction has 3,933 files: 659 SWF, 543 FLA, 2,711 MP3, 9 AS, 9 XML,
  and 2 JPG. All 2,187 canonical same-path files match exactly and no same-path
  byte conflict was found. The quarantine recovers all 141 unique missing
  Grade 5 SWFs, covering 142 missing page occurrences; 112 have a paired FLA
  and 29 are SWF-only. Current canonical source coverage is 418/560 page
  occurrences plus 9/9 shells = 427/569; reviewed-promotion potential is
  569/569.
- The elementary DIG ZIP is 92,213,676 bytes with SHA-256
  `e367ea90c904894080c4c8e11f9eaaaebf615e14b655991b68820977ecbd6428`.
  It contains 797 SWF and 797 FLA. Unique case-insensitive matches recover
  316/317 currently missing Key Term references; `Polynomial.swf` remains
  missing while `polynomial.fla` exists, so potential coverage is 759/760.
- Folder ZIP exports do not expose stable per-file Drive IDs. Preserve the real
  container folder ID, archive hash, manifest path, file hash, and reviewed
  case/variant decision; never synthesize entry IDs.
- Signed-in visible-tree inspection found no additional missing auxiliary
  path in `HELP_AS_FILES`, `HELP_FORMULAS`, non-DIG Key Terms, or the
  `HELP_COURSES` root. Redundant auxiliary downloads were blocked and no file
  was created, so this conclusion is a path/name inventory plus current local
  hash/catalog audit, not a cloud-byte hash attestation.
- This intake changes source availability only. It does not promote canonical
  sources or prove JavaScript implementation, Flash fidelity, audio, human
  review, owner approval, strict completion, or publication.

## Current Project Memory: Grade 4 Active-Source Promotion — 2026-08-02

- Transaction `20260802T040219914Z-1635d216a5c2` committed the reviewed Grade 4
  active dependency closure: 202 active-page SWFs, 143 new same-path FLAs, and
  883 source-bound MP3s, or 1,228 new files totaling 434,656,573 bytes. Twenty
  additional same-path FLAs, 854 MP3s, and nine lesson XML files were retained
  as exact canonical bindings.
- The immutable plan SHA-256 is
  `61fbb021fbab57c427e1c0459c30cf94a88b449d0080c125d616213687833a87`;
  the immutable applied-receipt SHA-256 is
  `df23e474a6a8ab632b5e7ed6928a485427ed8d1873fb846c2f79d06dfd0c0f72`.
  The remaining 1,022 of 2,092 generic candidate rows were not promoted.
- The rebuilt frozen tree contains 9,147 files and 3,214,585,414 bytes. Its
  source-manifest SHA-256 is
  `f0a33c8a3d15afd7340e9ea5523385428bae7546bd8d4227a3a8977ab8914318`;
  its catalog checksum-set SHA-256 is
  `30dfa12b7cd76e7200fb89115155e7d32af1356247c07e3a4f79227e93f34875`.
- Grade 4 canonical source coverage is now 645/645 active pages plus 12/12
  shells = 657/657. Sixteen source-derived MP3 paths remain absent, so audio
  dependency closure, audio fidelity, and all runtime/acceptance/release gates
  remain open.
- The pre-promotion source and catalog roots remain retained and independently
  verified at the exact 7,919-file base identity. The first staging attempt,
  which failed before any swap because Node's clone API returned `ENOSYS`, is
  also retained as a rolled-back transaction record rather than deleted.

## Current Project Memory: Grade 4 Course Continuation

- CEO review approved the current lesson-player interaction direction, but
  that stakeholder approval does not close Flash fidelity or release gates.
- Grade 4 contains 12 lesson shells and 645 active lesson-page references, or
  657 required renderer members. Current canonical source coverage is 657/657
  after the reviewed active-source promotion and catalog rebuild. This source
  custody result does not add or authorize a JavaScript renderer.
- The dated current-working-tree JavaScript registry covers 41 page renderers
  and 2 shell renderers = 43/657. It still lacks 604 page renderers and 10
  shell renderers = 614 members. L1 contributes 1 page + 1 shell, L3 contributes
  39 pages + 1 shell, and L9 contributes 1 page; only L3 is renderer-complete.
- The product must derive whole-lesson descriptors from the source-backed
  catalog, preserve exact page order and repeated placements, and fail closed
  whenever a page renderer is absent; it must never display a placeholder as
  migrated content. Recalculate coverage after catalog or registry changes.
- The generic course route and player should preserve the reviewed G4 L3 and
  G5 L4 behavior while allowing each Grade 4 descriptor to be added without
  route duplication. Existing lesson-specific companions remain lesson-owned.
- `apps/web/lib/g4-course-catalog-coverage.server.ts` is the fail-closed planning
  entry and is intentionally not imported into the public route registry.
- Grade 4 Lesson 10 work has started with the standard preserved-source draft
  workspace `migrations/course-g04-l10-vb-003`; it contains no renderer,
  baseline, audio decision, fidelity result, strict completion, whole-lesson
  route, or release claim.
- The 2026-08-02 work starts the full Grade 4 generation program; it does not
  mean all 645 pages or 12 shells have passed migration, original-runtime,
  audio, visual, human, owner, strict-completion, or release acceptance.

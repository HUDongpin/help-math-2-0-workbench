# Session memory — Flash-to-JavaScript migration factory

Thread: `019f8141-40bc-7513-9a91-655f59e18db1`

## Purpose

Inventory the complete HELP Math Flash archive, build a reusable migration
factory, establish strict evidence gates, scaffold representative pilots, and
prepare controlled batch migration.

## Durable inventory and architecture

The session established a frozen archive and machine-readable catalog with:

- 7,919 legacy files and 2,779,928,841 bytes;
- 1,894 SWF placements and 1,873 canonical SWF assets;
- 21 duplicate placements retained with separate provenance;
- 32,149 root-timeline frames, all reported at 12 FPS;
- 1,181 paired FLA/SWF assets, 713 SWF-only assets, and 217 additional
  FLA-only items;
- 77 catalog batches of at most 25 canonical assets;
- 16 representative pilot workspaces and fail-closed completion ledgers.

These counts are historical evidence and should be reverified after the
external-drive copy.

## Acceptance state

- The workbench, catalog, validators, ledgers, batch gates, dynamic runtime
  registry, machine audits, and product routes were implemented.
- The pilot program repeatedly remained strict 0/16 even when tests, builds,
  static audits, current-JavaScript captures, or selected RMSE candidates
  passed.
- Current-JavaScript approval was hash-bound and intentionally narrower than
  original Flash fidelity, audio, full interaction, human visual review, owner
  acceptance, and strict completion.
- Protected VB004 and other evidence pins were allowed to fail rather than being
  overwritten without the required independent approval.
- G4 L3 machine evidence and construction cards were prepared, but batch gates
  were not allowed to bypass strict prerequisites.

## Source-faithful rules

- Preserve `adapter -> random -> source event -> frame -> operation`.
- `runtime.frameCount` is the root timeline; nested MovieClip frame domains must
  remain separate.
- Ruffle is a forensic reference, not a strict baseline.
- FFDec/swfmill structural output is not original-runtime behavior.
- Candidate screenshots, current JavaScript output, authoritative baseline,
  audio review, human review, owner review, strict admission, and publication
  are separate records.
- Do not infer missing Spanish audio, host entry state, random branches, or
  external calls.

## User preference carried forward

The user ultimately asked that the session stop giving repeated engineering
time estimates and report only actual progress, evidence state, and blockers.

## Next work

Reverify catalogs and hashes on the external disk, run the current workbench
gates, inspect protected failures without rewriting them, then resume from the
first evidence/implementation gate that is genuinely current.


# G4 L10 Native Helper v2.14 Independent-Review Chunk Plan v6

Status: `FROZEN_BEFORE_NEW_TASK_CREATION`; one-use new-batch transport and evidence plan only. It creates no task, HMG4RB4, verdict, implementation/test/execution, protected installation, original-runtime launch, apply/recover, acceptance, promotion, integration, release, or publication authority.

Frozen at: `2026-08-07T00:26:33Z`

This no-clobber successor does not reuse, repair, validate, cure, or incorporate any prior task ID, HMG4RB4, review command, output, finding, or result. V5's 21 contract-content chunks avoided observed terminal truncation, but batch `ab155b63...` failed permanently. V6 preserves only the independently rechecked byte boundaries and corrects the xattr measurement ambiguity: the NUL-containing native value is represented only by `xattr -px` followed by native `tr -d '[:space:]'`; `xattr -p` raw stdout is forbidden for length evidence.

## 1. Exact target and root transport snapshot

```text
absolute-path=/Volumes/WestWorld/HELP MATH 2.0/docs/G4_L10_NATIVE_HELPER_V2_14_SECURITY_CONTRACT_SUCCESSOR.md
SHA-256=a86c726ca5e3ae89cfb110c1a3dedb751c3cb2c51d1b737a908a91ddd0bf9510
UTF-8-bytes=50310
LF-count=173
final-LF=1
type=Regular File
mode=0444
nlink=1
inode=5914134
device=16777244
uid=501
gid=20
flags=-
mtime-epoch=1786019793
xattr-name-count=1
xattr-name-literal=com.apple.provenance
xattr-name-file-bytes-including-LF=21
xattr-name-file-SHA-256=aacae6d7f20ecc718526220d315bd6ab0a28e8aa5da91934df496b54c45908c5
xattr-value-canonical-uppercase-hex=010200220211CD1E32E5B0
xattr-value-canonical-hex-bytes=22
xattr-value-canonical-hex-LF=0
xattr-value-canonical-hex-SHA-256=305e0ad6eaebe2d11360bf0770f06a2c21ffcd373f36abf66ed91452c480eb69
xattr-value-logical-bytes=11
ACL-entry-count=0
```

This parent snapshot is transport metadata, not reviewer evidence. Each reviewer must establish its own before and immediate-final after identity. In this environment, native `xattr -p` raw stdout reports only the three bytes before the embedded NUL and is therefore prohibited for value-length evidence. Native `xattr -px | tr -d '[:space:]'` yields the exact 22-character canonical hexadecimal projection above; it represents 11 logical xattr bytes. Drift, unexpected xattr, quarantine, ACL entry, symlink, wrong type, unavailable observation, or ambiguity fails the unit.

## 2. Mandatory non-evidence diagnostic

Before any target read or evidence-bearing command, each reviewer must label and execute exactly this command body from its authenticated start envelope:

```zsh
for tool in /usr/bin/shasum /usr/bin/wc /usr/bin/iconv /usr/bin/stat /usr/bin/xattr /bin/ls /usr/bin/sed /usr/bin/tr; do [[ -x "$tool" ]] || { print -r -- "MISSING:$tool"; exit 1; }; done
print -r -- 'DIAGNOSTIC_ONLY_NOT_EVIDENCE:macOS-native-tools-available'
```

Expected stdout is exactly one LF-terminated line:

```text
DIAGNOSTIC_ONLY_NOT_EVIDENCE:macOS-native-tools-available
```

Any different body, missing tool, nonzero exit, extra output, ambiguity, or correction permanently invalidates the unit. Python filesystem/xattr inspection, including `os.listxattr`, is forbidden. Heredocs are forbidden for byte, LF, identity, chunk, preimage, output, or HMG4RB4 accounting.

## 3. Frozen terminal-output non-truncation scheme

The 50,310-byte contract is partitioned at existing LF boundaries into exactly 21 contiguous chunks. Every byte occurs once, in order. Each chunk is at most 3,072 bytes; observed maximum is 3,037. Boundaries may not be discovered, split, merged, reordered, or replaced dynamically.

For every terminal-visible contract-content read:

1. Emit exactly one listed chunk and no other contract content in that tool result.
2. Never use aggregate `cat`, wildcard, loop, command substitution, multi-range command, or combined stdout that emits more than one chunk.
3. Use one separate tool call per display with output allowance at least 12,000 tokens. Metadata commands must emit at most 120 lines and 8,000 bytes.
4. Any truncation warning, omitted-line marker, ellipsis, incomplete UTF-8 sequence, missing expected LF, or ambiguity permanently invalidates the unit; retry cannot cure it.
5. Extract each chunk to a reviewer-unique `/tmp` file, verify byte/LF/SHA without displaying content, then display only the same native `sed` range.
6. Never print the reconstructed full contract. Cite chunk IDs and verified hashes.

Columns: `chunk|first-line|last-line|zero-based-byte-offset|bytes|LF|SHA-256`.

```text
001|1|41|0|3003|41|e5c644a95dc68d8bdd33e9b0283e67aa567464d73cc0a96bfcfc0f46101ca0d2
002|42|84|3003|2886|43|dbfa82045e1b8f864e6273daeee6346ebebc83ea1e58cdd253e15f164814de44
003|85|89|5889|2002|5|184f8988d4e03f1c4dead64a352eb91bb8882a95c03fd7045216e4a39bc489f1
004|90|92|7891|2938|3|e229411dde92919394e59c066d4f04a776805683c2224983982a9aa5668465c6
005|93|95|10829|2261|3|d4fae3b8707294eefcec3b6bd14ed94159093c7fa482a0a7e7c53bcb92ed0306
006|96|97|13090|2070|2|e9626555e02ecf662a888ed8ac41b7f707ecc0b99f588c3aacc255ce6c42659e
007|98|98|15160|1599|1|e106cdbdc7dfe767d586d8767076a5c05ab2f8aa8b1f04592de458aea28621c5
008|99|99|16759|2702|1|e966f00282a96f1a68fec7f45ae321acde5076b18e66a93b1b1a5c26f4510429
009|100|100|19461|2484|1|162d0c01d639a67f80c4844a644c999bb688bec9cf518cea079dcc1301d93387
010|101|101|21945|2969|1|519c93c7c881f4157c669dbb47b131d2498c271fa344407fee1a7ed21fa871f7
011|102|102|24914|2279|1|bb409aaac74610f67653eb768f6c4c9c82cb02b898f951f448639fcc083e4d47
012|103|103|27193|2203|1|b60cc2d1e6ead8429227bbbe216ef30be269e21d1e36f944e225140b7282bd64
013|104|104|29396|1905|1|92b44fb07bab44d5ef7a5b1cd6c6761a40616d1a851a269cb11f4db9342317bf
014|105|105|31301|2015|1|6c858f49a711d6d50945eeab126ed54fa8809167c26a002b78c3a8507ca89c96
015|106|106|33316|2455|1|a0588c3851b69dfe820d483a16e97e174edc48a0fb1d1ed5db91812b1424f086
016|107|107|35771|2111|1|6b7e3c1c462c4793b2259a69c5c197636856463e6ce35316c982ac854bf4131b
017|108|126|37882|2988|19|d4b0379108a8614cca3adbd5e906ee80a0d04cf4b3232422f65b62bc9be4975e
018|127|146|40870|3026|20|857a3939c988178fac9aaa867a09b962feee12dd6df3e800818462560fe070ef
019|147|158|43896|2827|12|1754f09b93d4dec51f1595a068d4492279646407ce9a5ec0d4d013c7337317bb
020|159|172|46723|3037|14|664a91bdcf4efc28e6b7de0c995de6c94b5e1aea142baf17b29544020f28801a
021|173|173|49760|550|1|6c46e5c043d0a6abd22516352411b4eefa8da2a8d312de95edd8c3e7143ce56a
```

```text
chunk-count=21
first-offset=0
final-end-offset=50310
sum-bytes=50310
sum-LF=173
ordered-concatenation-SHA-256=a86c726ca5e3ae89cfb110c1a3dedb751c3cb2c51d1b737a908a91ddd0bf9510
```

## 4. Fixed evidence sequence and identity method

Each reviewer uses a unique `/tmp` directory made by `/usr/bin/mktemp -d` and writes nothing in the workspace. Before each evidence command, state its label and exact claims. A failed, ambiguous, corrected, or truncated evidence command permanently invalidates the unit.

1. Before identity: native `shasum -a 256`; `wc -c`; `tr -cd '\n' | wc -c`; UTF-8 round-trip with `iconv` into the unique temp directory plus byte/hash comparison; `stat -f`; xattr names redirected to `xattr-names.txt`; xattr value canonicalized only with `xattr -px com.apple.provenance "$target" | tr -d '[:space:]' > xattr-value.hex`; and `ls -ldeO@`. Verify the frozen name/value file bytes and hashes. Do not use `xattr -p` raw length.
2. Chunks 001-021: extract exactly `/usr/bin/sed -n '<first>,<last>p' "$target" > "$review_tmp/chunk-NNN.bin"`; verify that file; in a separate call display exactly the same one `sed` range with output allowance >=12,000 tokens.
3. Reconstruct using `/bin/cat` with the explicit ordered filenames below, no wildcard or loop. Verify bytes/LF/UTF-8/SHA without displaying it.
4. Perform only the assigned Section 3 scope with reviewer-authored fail-closed commands. A command-design failure is permanent and disclosed.
5. Immediately before final output, repeat the complete original-target identity and compare before/after.

Explicit concatenation order:

```text
chunk-001.bin chunk-002.bin chunk-003.bin chunk-004.bin chunk-005.bin chunk-006.bin chunk-007.bin
chunk-008.bin chunk-009.bin chunk-010.bin chunk-011.bin chunk-012.bin chunk-013.bin chunk-014.bin
chunk-015.bin chunk-016.bin chunk-017.bin chunk-018.bin chunk-019.bin chunk-020.bin chunk-021.bin
```

## 5. Hold-only creation, new HMG4RB4, and authenticated start

This plan contains no new task ID or HMG4RB4. It must be hashed, reconstructed from all 21 chunks, and changed to mode `0444` before task creation. Exactly three fresh user-owned tasks are then created in order: `schema`, `adversarial`, `whole`. Creation prompts are hold-only: no command, file read, memory, review, or evidence action before the authenticated start envelope.

After all three task-system IDs exist, the parent rejects every prior ID, confirms the three are nonempty/distinct with no NUL/CR/LF, and computes HMG4RB4 from exactly five LF-terminated lines:

```text
HMG4RB4
a86c726ca5e3ae89cfb110c1a3dedb751c3cb2c51d1b737a908a91ddd0bf9510
NEW schema task ID
NEW adversarial task ID
NEW whole task ID
```

Start envelopes bind contract path/SHA, this plan path/SHA, HMG4RB4, all ordered IDs, recipient scope, HMG4GL4/HMG4AL3/HMG4PE1/HMG4FR3, unresolved V28, chunk rules, and closed authority. A task acting before its envelope or using any prior identifier is invalid.

Forbidden prior task IDs include every ID in V5 plus:

```text
019fd977-d18d-7ad3-8a21-cbd9100647b1
019fd978-0063-7581-81ac-613fcdc93498
019fd978-1c3b-71f2-bdf5-40780fe47c92
```

Forbidden prior HMG4RB4 values include every value in V5 plus:

```text
ab155b63e1ffd8bdf588b0e5b69072e42542dabe99c936adbc1ad8caff289e0a
```

The full V5 forbidden sets remain normative by exact predecessor identity `55e21837abeb83cddc2653e0ed9085454061249f7b48499ae2428dbd328e339a`. Each new ID and batch is one-use; no replay, mixing, repair, or reuse.

## 6. Section 3 review scopes and output minimum

- `schema`: independently reconstruct HMG4GL4, validate HMG4AL3, decode/hash all 42 HMG4PE1 paragraphs, compare every row/edge/target, and audit inherited production-contract grammar.
- `adversarial`: attack ownership, replay/mixed-batch/root spoofing, original-output availability, Base64/length/hash aliases, command-failure disclosure, user authentication, V28 preservation, clean-room isolation, and every implementation/runtime authority path.
- `whole`: read byte 1 through EOF, validate full lineage and exclusions, independently repeat structural/all-42 checks, and confirm no authority expansion.

Every final output must echo own ID, ordered IDs, HMG4RB4, scope, contract/plan identities, before/after target identity, P0/P1/P2 with all findings or explicit zero, HMG4GL4, HMG4AL3, all-42 HMG4PE1, HMG4FR3, unresolved operational-freeze state, complete command/error/truncation disclosure, completion result, and closed no-authority. Any nonzero, omitted, unevaluated, ambiguous, corrected, truncated, or invalidated unit is not zero.

## 7. Closed authority

These tasks perform read-only independent contract review only. They may not edit the workspace, create or alter a contract, implement or test a helper, inspect or reuse retired implementation knowledge, mutate V28, install, launch original runtime, apply, recover, accept, promote, integrate, release, or publish. Even three `P0/P1/P2=0/0/0` results cannot authorize production-helper implementation. That requires a separate authenticated post-review instruction and every retained v2.14 Section 4 gate.

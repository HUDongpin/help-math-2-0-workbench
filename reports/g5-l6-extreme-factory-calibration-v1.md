# G5 Lesson 6 extreme factory calibration v1

G5 L6 now has canonical source custody for **40/40 active page placements**. The promotion added exactly 13 SWFs and 10 same-path FLAs; the legacy Flash course shell remains excluded. This source success does not make L6 a static factory batch.

The extreme slice `course-g05-l06-ti-003` is an explicit **NO-GO for source-static generation**:

- SWF-only source: 270,373 bytes, SHA-256 `b80e215e50b7d24be61fe5b53ba6cd1f563a300381dc728613c7a6bf4248062b`.
- Declared root: 800×600, 12 fps, 10 frames.
- Actual machine structure: AS1/2; 1,355 exported script files; **1,309 FFDec `CLIPACTIONRECORD` paths**; 1,748 events; 2,274 placements; 489 streaming-audio blocks; 25 morphs; 88 nested definitions; 9 nested timelines longer than the root; maximum nested length 490 frames.
- Catalog also associates exact `L6TI03.mp3` bytes, but required cue mapping, language, synchronization, and listening acceptance have not been established.

All FFDec and swfmill extraction commands succeeded, source hashes matched before and after, and the two tools agree on the SWF header. Those facts establish the extreme structural risk; they do not execute or validate the original interaction system.

## Decision

Do not create a root-only Canvas adapter and do not use TI003 to authorize L6 scale-out. The next valid lane is behavior-first: authoritative original-runtime natural traces, source-derived behavior/host contracts, full reachable-domain disposition, embedded and external audio synchronization, deterministic branch fixtures, and interaction-state visual review.

No renderer or registry entry was created. Current‑JS, original-runtime fidelity, behavior parity, visual fidelity, audio acceptance, human review, Owner acceptance, strict completion, release, and publication all remain false.
